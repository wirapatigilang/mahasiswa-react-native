# Architecture & Flow Diagram

## 🏗️ Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         APLIKASI MAHASISWA                       │
│                        (React Native - Expo)                     │
└─────────────────────────────────────────────────────────────────┘
                                 │
                    ┌────────────┴────────────┐
                    │                         │
          ┌─────────▼─────────┐     ┌────────▼─────────┐
          │   Authentication   │     │   Data Layer     │
          │      Layer         │     │                  │
          │                    │     │                  │
          │  - Login           │     │  - Mahasiswa     │
          │  - Register        │     │    Service       │
          │  - Logout          │     │  - Firestore     │
          │  - Session Mgmt    │     │    Operations    │
          └─────────┬──────────┘     └────────┬─────────┘
                    │                         │
          ┌─────────▼─────────┐     ┌────────▼─────────┐
          │   AsyncStorage     │     │   Firebase        │
          │                    │     │   Firestore       │
          │  - User Session    │     │                  │
          │  - Auto-login      │     │  - mahasiswa     │
          │    Data            │     │    collection    │
          └────────────────────┘     └──────────────────┘
```

## 📱 Screen Flow

```
┌──────────────┐
│  App Launch  │
└──────┬───────┘
       │
       ▼
┌──────────────────┐
│ Check AsyncStorage│
└──────┬───────────┘
       │
       ├─── No Data ───┐
       │               │
       │               ▼
       │        ┌─────────────┐      ┌──────────────┐
       │        │ LoginScreen │◄─────┤RegisterScreen│
       │        └─────┬───────┘      └──────┬───────┘
       │              │                     │
       │              └──────┬──────────────┘
       │                     │ (Register/Login Success)
       │                     │
       └─── Has Data ────────┤
                             │
                             ▼
                   ┌──────────────────┐
                   │ MahasiswaScreen  │
                   │                  │
                   │ - User Info      │
                   │ - Data Mahasiswa │
                   │ - Pull Refresh   │
                   │ - Logout Button  │
                   └──────────────────┘
```

## 🔄 Data Flow

### 1. Login Flow

```
User Input (Email + Password)
         │
         ▼
loginUser(email, password)
         │
         ├─► Firebase Authentication
         │   └─► Verify Credentials
         │
         ▼
   Login Success
         │
         ├─► Save to AsyncStorage
         │   └─► {uid, email, displayName, photoURL}
         │
         ▼
Navigate to MahasiswaScreen
         │
         ▼
Fetch Mahasiswa Data from Firestore
```

### 2. Auto-Login Flow

```
App Launch
    │
    ▼
getUserFromStorage()
    │
    ├─► Read AsyncStorage
    │   └─► Key: '@user_session'
    │
    ▼
User Data Found?
    │
    ├─── Yes ──► Set User State ──► MahasiswaScreen
    │
    └─── No ───► LoginScreen
```

### 3. Fetch Mahasiswa Flow

```
MahasiswaScreen Mounted
         │
         ▼
getMahasiswa()
         │
         ▼
Firebase Firestore Query
         │
         ├─► collection('mahasiswa')
         │
         ├─► orderBy('nim', 'asc')
         │
         ▼
getDocs(query)
         │
         ▼
Transform Data
         │
         ├─► Add document ID
         │
         ▼
Return {success: true, data: [...]}
         │
         ▼
Update UI State
         │
         ▼
Display in FlatList
```

### 4. Logout Flow

```
User Click Logout
         │
         ▼
Confirm Dialog
         │
         ▼
logoutUser()
         │
         ├─► Firebase signOut()
         │
         ├─► AsyncStorage.removeItem('@user_session')
         │
         ▼
Clear User State
         │
         ▼
Navigate to LoginScreen
```

## 🗄️ Data Structure

### AsyncStorage

```javascript
Key: '@user_session'
Value: {
  "uid": "firebase_user_id_123",
  "email": "user@example.com",
  "displayName": "User Name",
  "photoURL": "https://..."
}
```

### Firestore - mahasiswa Collection

```javascript
Document {
  id: "auto_generated_id",
  nim: "2021001",
  nama: "Ahmad Rizki",
  jurusan: "Teknik Informatika",
  semester: 5,
  createdAt: "2024-01-15T10:00:00.000Z"
}
```

## 🔐 Authentication States

```
┌─────────────────┐
│   INITIAL       │
│  (checking)     │
└────────┬────────┘
         │
         ▼
    ┌────────┐
    │ Check  │
    │Storage │
    └───┬────┘
        │
        ├─── Found ────┐
        │              │
        ▼              ▼
 ┌──────────┐   ┌─────────────┐
 │UNAUTHENTICATED│ AUTHENTICATED│
 │              │ │             │
 │ - LoginScreen│ │ - Mahasiswa │
 │ - RegisterScr│ │   Screen    │
 └──────────────┘ └─────────────┘
        ▲              │
        │              │
        └──── Logout ──┘
```

## 🎯 Component Hierarchy

```
App.js
├── NavigationContainer
│   ├── (if NOT authenticated)
│   │   └── Stack.Navigator
│   │       ├── LoginScreen
│   │       │   ├── TextInput (Email)
│   │       │   ├── TextInput (Password)
│   │       │   └── TouchableOpacity (Login Button)
│   │       └── RegisterScreen
│   │           ├── TextInput (Email)
│   │           ├── TextInput (Password)
│   │           ├── TextInput (Confirm Password)
│   │           └── TouchableOpacity (Register Button)
│   │
│   └── (if authenticated)
│       └── MahasiswaScreen
│           ├── Header
│           │   ├── User Email
│           │   └── Logout Button
│           └── FlatList
│               └── MahasiswaCard (Item)
│                   ├── NIM Badge
│                   ├── Nama
│                   ├── Jurusan
│                   └── Semester
```

## 🔌 Service Layer

```
┌──────────────────────────────────────┐
│         authService.js               │
├──────────────────────────────────────┤
│ registerUser(email, password)        │
│ loginUser(email, password)           │
│ logoutUser()                         │
│ getUserFromStorage()                 │
│ saveUserToStorage(user)              │
│ checkAuthState(callback)             │
└───────────────┬──────────────────────┘
                │
                ├─► Firebase Auth SDK
                └─► AsyncStorage

┌──────────────────────────────────────┐
│      mahasiswaService.js             │
├──────────────────────────────────────┤
│ getMahasiswa()                       │
│ addMahasiswa(data)                   │
│ updateMahasiswa(id, data)            │
│ deleteMahasiswa(id)                  │
└───────────────┬──────────────────────┘
                │
                └─► Firebase Firestore SDK
```

## 📊 State Management

```
App.js State:
├── user: User | null
│   └── Contains: {uid, email, displayName, photoURL}
│
└── loading: boolean
    └── Shows loading indicator while checking auth

MahasiswaScreen State:
├── mahasiswaList: Array<Mahasiswa>
│   └── Data from Firestore
│
├── loading: boolean
│   └── Initial load indicator
│
└── refreshing: boolean
    └── Pull-to-refresh indicator
```

## 🌐 Network Requests

```
┌─────────────────────────────────────────────┐
│           Client (React Native App)          │
└────────────────┬────────────────────────────┘
                 │
    ┌────────────┼────────────┐
    │            │            │
    ▼            ▼            ▼
┌────────┐  ┌────────┐  ┌──────────┐
│Firebase│  │Firebase│  │Local     │
│  Auth  │  │Firestore│ │Storage   │
│  SDK   │  │  SDK   │  │(AsyncStor)│
└────┬───┘  └────┬───┘  └────┬─────┘
     │           │            │
     ▼           ▼            ▼
┌─────────────────────────────────┐
│      Firebase Backend           │
│                                 │
│  - Authentication Server        │
│  - Firestore Database           │
│  - Security Rules Engine        │
└─────────────────────────────────┘
```

## 🎨 UI Component Breakdown

### LoginScreen

```
┌─────────────────────────────────────┐
│         📱 LOGIN SCREEN              │
├─────────────────────────────────────┤
│                                     │
│           [  Login  ]               │
│                                     │
│  ┌───────────────────────────────┐ │
│  │ Email                         │ │
│  └───────────────────────────────┘ │
│                                     │
│  ┌───────────────────────────────┐ │
│  │ Password         (hidden)     │ │
│  └───────────────────────────────┘ │
│                                     │
│  ┌───────────────────────────────┐ │
│  │        [ LOGIN ]              │ │
│  └───────────────────────────────┘ │
│                                     │
│   Belum punya akun? Daftar disini  │
│                                     │
└─────────────────────────────────────┘
```

### MahasiswaScreen

```
┌─────────────────────────────────────┐
│  Data Mahasiswa      [ Logout ]     │
│  user@email.com                     │
├─────────────────────────────────────┤
│ ┌─────────────────────────────────┐│
│ │ 2021001          [badge]        ││
│ │ Ahmad Rizki                     ││
│ │ ────────────────────────────    ││
│ │ Jurusan: Teknik Informatika     ││
│ │ Semester: 5                     ││
│ └─────────────────────────────────┘│
│ ┌─────────────────────────────────┐│
│ │ 2021002          [badge]        ││
│ │ Siti Nurhaliza                  ││
│ │ ────────────────────────────    ││
│ │ Jurusan: Sistem Informasi       ││
│ │ Semester: 5                     ││
│ └─────────────────────────────────┘│
│                                     │
│    (Pull down to refresh)           │
└─────────────────────────────────────┘
```

## 🚀 Performance Considerations

```
┌─────────────────────────────────────┐
│    Optimization Strategies           │
├─────────────────────────────────────┤
│                                     │
│ 1. FlatList for Mahasiswa           │
│    └─► Efficient rendering          │
│                                     │
│ 2. AsyncStorage for Session         │
│    └─► Fast local access            │
│                                     │
│ 3. Pull-to-Refresh                  │
│    └─► Manual data refresh          │
│                                     │
│ 4. Loading States                   │
│    └─► Better UX                    │
│                                     │
│ 5. Error Boundaries                 │
│    └─► Graceful error handling      │
│                                     │
└─────────────────────────────────────┘
```

---

**Note:** Diagram ini menjelaskan arsitektur dan flow aplikasi untuk memudahkan development dan maintenance.
